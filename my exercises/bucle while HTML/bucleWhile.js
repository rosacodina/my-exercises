//Bucle while es como for, mientras se cumpla una condición se ejecutará el código que le digamos

function bucleWhile(num) {
  let i = 1;

  while (i <= num) {
    console.log(i);
    i++;
  }
}

bucleWhile(11);
